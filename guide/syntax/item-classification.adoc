.CPV を利用する場合の UBL での例
[source, xml, indent=0]
----
include::{snippet-dir}/item.xml[tags=cpv]
----
<1> listID は、UNCL7143 コード一覧からのものでなければならず、コード STI は、これが CPV 分類であることを示しています。

.UNSPSCの UBL での例
[source, xml, indent=0]
----
include::{snippet-dir}/item.xml[tags=unspsc]
----
<1> listID は、UNCL7143 コード一覧からのものでなければならず、コード TST は、これが　UNPSC　分類であることを示し、listVersionID　は任意ですが、UNPSC のバージョンを指定するために使用することができます。留意して欲しいのは、以前のバージョンでは、コード MP は、UNPSC を識別するための一時的な回避策として使用されていました。2019年秋リリースでは、UNSPSC に固有の新しい7143コード TST に置き換えられています。
