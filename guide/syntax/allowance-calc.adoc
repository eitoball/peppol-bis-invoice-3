

.基準額と割合が存在する場合の割引と手数料の計算の UBL での例
[source, xml, indent=0]
----
include::{snippet-dir}/Calc.xml[tags=allowance1]
----
<1> 基準額、割合と一緒に合計を計算します
<2> 手数料割合
<3> stem:["基準額" times ("割合" div 100) = "合計"]


.基準額と割合が存在しない場合の割引と手数料の計算の UBL での例
[source, xml, indent=0]
----
include::{snippet-dir}/Calc.xml[tags=allowance2]
----
<1> 基準額と割合での計算がない場合の割引額